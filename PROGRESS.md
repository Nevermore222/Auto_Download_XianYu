# 当前开发进度

> 最后更新：本提交

## 已完成功能

- **闲鱼视频代下载业务**：客户提供视频链接 → 服务器解析、下载 → 上传夸克网盘 → 返回分享链接
- **夸克网盘**：整块上传（≤1MB）+ 分片上传（>1MB）、创建分享链接；支持在指定目录下创建批次文件夹（`create_batch_folder`）、按 `pdir_id` 上传
- **卖家工作台（Web）**：单页输入链接（支持多条，每行一条）→ 批量查询价格 → 提交并处理 → 显示进度步骤 → 复制夸克链接；移动端适配
- **批量处理**：多条链接统一估价、下载到同一本地批次文件夹、上传到夸克同一批次文件夹、生成一个分享链接；每条下载使用 `batch_index` 避免同名覆盖
- **进度日志**：下载→上传夸克→生成链接各步骤通过 `progress_callback` 上报，前端轮询显示
- **核销码（桌面端）**：生成/校验/扣次，客户绑定核销码后按次下载，次数用尽失效；桌面端不单独收费
- **桌面端应用**：CustomTkinter 界面，解析视频、绑定核销码、下载到本地；可 PyInstaller 打包 exe
- **配置与文档**：`config.yaml`、夸克 Cookie 获取说明、简单操作说明、卖家流程与夸克上传说明

## 待优化 / 后续可做

- 夸克接口若变更可参考 CSDN 等文章调整 `src/quark/client.py`
- 负载均衡：多实例时任务状态可迁至 Redis
- 支付：如需真实支付后再下载，可对接支付接口或服务端校验
